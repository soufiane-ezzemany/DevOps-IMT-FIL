upstream loadbalancer {
    # Machines onto which nginx should balance the incomming load of requests
    server address1-to-modify;
    server address2-to-modify;
}
server {
    # Server configuration
    listen 0.0.0.0:80;
    location / {
    proxy_pass http://loadbalancer;
}}
