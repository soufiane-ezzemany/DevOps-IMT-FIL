apiVersion: batch/v1
kind: Job
metadata:
  name: seed
spec:
  template:
    spec:
      containers:
        - name: seed
          image: seed:latest
          imagePullPolicy: Never
      restartPolicy: Never
  backoffLimit: 4
